# TDLib Media Uploader 1.9.4 正式版

1.9.4 重点提升了上传预览与实际发送的一致性、异常结果的恢复保护，并加入可配置的视频封面截图时间。它也收紧了媒体扫描格式，让扫描到的内容与 Telegram 的视频/图片消息类型保持一致。

## 上传预览、配置与发送一致

- 上传任务使用用户确认过的预览计划、文件快照和独立配置快照。扫描后新加入的文件不会悄悄进入当前任务；文件在扫描后被替换或修改时，会在发送前被识别并拦截。运行中的任务继续使用启动时的配置，新任务读取保存后的最新设置。
- 多个设置入口之间会同步配置变化。没有冲突的编辑可以合并；同一字段被同时修改时会提示冲突，避免旧面板静默覆盖新值。上传任务运行期间，配置锁也会保护设置持久化。
- 上传前固定 Album 的完整成员和边界。单个文件的预检失败或暂时延迟，不会导致后续文件挪进前一个 Album，保证预览、断点和实际发送沿用同一计划。

## 断点恢复与结果核对

- TDLib 确认消息后会尽快保存对应消息 ID；部分成功、超时、取消或结果不完整时，会保留已知结果和未确认保护信息，减少重复发送风险。
- 未确认记录绑定文件快照，重新分组或调整配置不会绕过保护。人工恢复沿用记录中的来源目录并保留已有断点；旧记录没有来源目录时要求选择正确目录，状态写入失败时继续保留保护记录。
- 未确认上传页面提供文件、来源、目标和结果信息，便于在 Telegram 中核对后再处理不确定结果。

## 可配置的视频封面截图时间

- 视频和混合上传都可以分别设置从视频第几秒抽取封面，支持 0.01 秒精度，默认 `1.00` 秒。超出视频时长或指定位置抽帧失败时，程序回退到 `0.00` 秒；不会因此直接中断视频上传。
- 缩略图缓存仍沿用原有缓存规则。修改截图时间后，已缓存的视频可能继续显示旧封面；如需重新生成，请在设置中执行“仅清理视频封面”。无需清理上传状态或其他缓存。

## 视频和图片格式

- 视频扫描限定为 `.mp4`，图片扫描限定为 `.jpg`、`.jpeg` 和 `.png`。MP4 容器内的 H.264 与 H.265/HEVC 都保留在视频消息上传链路中；不会仅凭 H.265 编码名过滤视频。
- 旧配置中的其他视频/图片扩展名会在加载时过滤。AVI、MKV、MOV、WMV、裸 `.hevc` 以及 WebP、BMP、TIFF、GIF、APNG 等格式不会进入当前 Photo/Video 扫描。需要上传这些格式时，可先转换为上述媒体格式，或通过 Telegram 文件方式发送。

## 稳定性与性能

- 修复登录等待超时处理、暂存缓存清理误删非受管文件、损坏标题数据被覆盖，以及窗口关闭时缓存统计线程未妥善退出的问题。
- 上传进度初始化改为线性计算，减少大任务启动时的进度计算开销；回归测试按功能模块整理，并修正 Windows 与 Unix 环境中的测试兼容性。
- 发布工作流通过版本 tag 启动；构建会校验版本与 tag 一致，生成 SHA-256 校验清单，并在 Windows x64 与 macOS arm64 的构建及打包自检通过后创建正式 Release。

本版本完整离线回归测试共 366 项，已全部通过。
