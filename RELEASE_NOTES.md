### 本次更新

- 统一文件名和目录排序：连续数字片段按自然数从小到大排序，`x.1`、`x.10`、`x.41`、`x.409`、`x.410` 保持自然顺序。
- 稳定混合 Album 边界：扫描后暂时不可读的文件只在本轮延期，不会让后续文件补位；混合根目录媒体会忽略并提示。
- 为网络目录增加文件快照、读探针、有限重试和可选本地暂存；ExifTool/FFmpeg 处理增加批次隔离与超时。
