## 1.9.4

- 上传使用已确认的预览快照和独立配置；扫描后新增文件不会自动加入，变化的文件会在发送前被拦截。
- 修复内联设置绕过任务锁、旧面板覆盖外部修改以及上传目标被运行中配置改变的问题。配置入口双向刷新：合并未冲突的待保存编辑；同一字段冲突时阻止静默覆盖并提示恢复或重新编辑。
- 未确认上传按文件快照保护，重新分组也不会绕过；TDLib 接收后立即保存消息 ID，保留部分成功和未知结果。
- 手动恢复使用原来源目录且不重置已有断点；旧记录缺少目录时要求明确选择，写入失败继续保留保护记录。
- 修复登录等待超时、暂存清理误删用户文件、损坏标题文件被覆盖，以及关闭窗口时缓存统计线程未停止的问题。
- 上传进度初始化改为线性计算；测试按功能拆分，移除重复的历史迁移索引。
- 发布只由版本标签触发，禁止覆盖已有 Release；固定 Actions 版本，统一资产名称与校验清单中的文件名。
